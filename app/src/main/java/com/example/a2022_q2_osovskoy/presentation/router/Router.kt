package com.example.a2022_q2_osovskoy.presentation.router

/*class Router {
    val navigationCommands: LiveData<NavigationCommand>
    fun navigateTo(screen: Screen) {}
}
class ScreenViewModel(router: Router) {
    fun handleOpenDetailsEvent() {
        router.navigateTo(Screens.SomeScreen)
    }
}
class Fragment {
    @Inject
    val router: Router
    fun onStart() {
        router.navigationCommands.observe(this) {
            // perform actual navigation,
            // probably using a helper Navigator class
        }
    }
}*/
